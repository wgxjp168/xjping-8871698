<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hd.auth.mapper.SysRoleMapper">

    <select id="selectRolesByUserId" resultType="com.hd.auth.entity.SysRole">
        SELECT r.id, r.role_code, r.role_name, r.description, r.status, r.create_time
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.status = 1
    </select>

</mapper>
