# ============================================
# business-logic-layer - 生产环境
# ============================================

business:
  data:
    init-sample-data: false
  cart:
    max-items-per-user: 99

logging:
  level:
    com.ilbuy.business: INFO

# 生产环境数据库配置（需替换为实际值）
# spring:
#   datasource:
#     url: jdbc:mysql://db-master.ilbuy.internal:3306/ilbuy_prod
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#     hikari:
#       maximum-pool-size: 20
#       minimum-idle: 5
#       connection-timeout: 30000
