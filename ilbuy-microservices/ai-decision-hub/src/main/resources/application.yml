server:
  port: 8081

spring:
  application:
    name: ilbuy-ai-decision-hub

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.ilbuy: DEBUG
