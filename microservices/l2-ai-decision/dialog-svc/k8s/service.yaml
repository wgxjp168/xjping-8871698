apiVersion: v1
kind: Service
metadata:
  name: dialog-svc
  namespace: ilbuy
  labels:
    app: dialog-svc
    component: l2-ai-decision
spec:
  type: ClusterIP
  selector:
    app: dialog-svc
  ports:
    - name: http
      protocol: TCP
      port: 8013
      targetPort: http
