apiVersion: v1
kind: Service
metadata:
  name: llm-svc
  namespace: ilbuy-l2
  labels:
    app: llm-svc
    component: ai-decision
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: llm-svc
  ports:
    - name: http
      protocol: TCP
      port: 8011
      targetPort: http
