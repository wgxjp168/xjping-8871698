# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1

# HTTP clients
httpx==0.28.1
aiohttp==3.11.10

# Scheduling
APScheduler==3.10.4

# Redis (proxy pool / job state)
redis[asyncio]==5.2.1

# Scraping (optional scrapy integration)
scrapy==2.12.0

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0

# Testing (dev only — not installed in Docker runtime stage)
pytest==8.3.4
pytest-asyncio==0.24.0
