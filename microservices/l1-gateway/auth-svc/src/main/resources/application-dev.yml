spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/ilbuy_auth?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
      username: root
      password: root
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 1

ilbuy:
  jwt:
    secret: ilbuy-platform-jwt-secret-key-2024-must-be-at-least-256-bits

logging:
  level:
    com.ilbuy.auth: DEBUG
    org.springframework.security: DEBUG
