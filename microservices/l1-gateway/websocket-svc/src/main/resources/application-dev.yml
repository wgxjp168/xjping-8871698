spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  data:
    redis:
      host: localhost
      port: 6379
      database: 2

ilbuy:
  jwt:
    secret: ilbuy-platform-jwt-secret-key-2024-must-be-at-least-256-bits

logging:
  level:
    com.ilbuy.websocket: DEBUG
