apiVersion: v1
kind: Secret
metadata:
  name: ilbuy-l1-secret
  namespace: ilbuy
type: Opaque
stringData:
  # 生产环境请使用 kubectl create secret 或 Sealed Secrets / Vault 管理
  JWT_SECRET: "REPLACE_WITH_STRONG_SECRET_AT_LEAST_256_BITS"
  REDIS_PASSWORD: "REPLACE_WITH_REDIS_PASSWORD"
  DB_PASSWORD: "REPLACE_WITH_DB_PASSWORD"
