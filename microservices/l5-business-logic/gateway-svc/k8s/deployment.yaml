apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-svc
  namespace: ilbuy-l5
  labels:
    app: gateway-svc
    layer: l5
    version: "1.0.0"
spec:
  replicas: 1  # prod: 2
  selector:
    matchLabels:
      app: gateway-svc
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: gateway-svc
        layer: l5
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8010"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: gateway-svc
      terminationGracePeriodSeconds: 60

      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: gateway-svc

      containers:
        - name: gateway-svc
          image: ilbuy/gateway-svc:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8010
              name: http

          # Environment variables from ConfigMap
          envFrom:
            - configMapRef:
                name: gateway-svc-config

          # Sensitive config from Secret
          env:
            - name: REDIS_PASS
              valueFrom:
                secretKeyRef:
                  name: ilbuy-l5-secrets
                  key: REDIS_PASS
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ilbuy-l5-secrets
                  key: JWT_SECRET

          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8010
            initialDelaySeconds: 45
            periodSeconds: 30
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8010
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3

          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]

          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: logs
              mountPath: /app/logs

      volumes:
        - name: tmp
          emptyDir: {}
        - name: logs
          emptyDir: {}
