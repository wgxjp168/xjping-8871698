# ===== Dev 环境配置 =====
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ilbuy_profile?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123

  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 2

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: dev
        group: DEFAULT_GROUP

logging:
  level:
    com.ilbuy: DEBUG
    org.springframework.security: DEBUG
    com.baomidou.mybatisplus: DEBUG
