# =========================================================
# physical-auth - DEV 环境配置
# =========================================================
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/physical_health?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: physical@2024
    hikari:
      minimum-idle: 2
      maximum-pool-size: 5
  redis:
    host: localhost
    port: 6379
    password:

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   # 开发环境打印SQL

county:
  api:
    base-url: http://localhost:8080   # 本地模拟县域接口

logging:
  level:
    com.health.physical: debug
    org.springframework.security: debug
