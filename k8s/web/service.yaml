apiVersion: v1
kind: Service
metadata:
  name: physical-web
  namespace: physical-health
spec:
  type: ClusterIP
  selector:
    app: physical-web
  ports:
    - name: http
      port: 80
      targetPort: 80
